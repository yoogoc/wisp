(
    version: 1,
    command: "dateseq",
    description: Some("Nifty command line date and time utilities; fast date calculations and conversion in the shell"),
    subcommands: [],
    options: [
        (
            names: [
                "--help",
                "-h",
            ],
            description: Some("Print help and exit"),
            takes_value: false,
            arguments: [],
        ),
        (
            names: [
                "--version",
                "-V",
            ],
            description: Some("Print version and exit"),
            takes_value: false,
            arguments: [],
        ),
        (
            names: [
                "--quiet",
                "-q",
            ],
            description: Some("Suppress message about date/time and duration parse errors and fix-ups"),
            takes_value: false,
            arguments: [],
        ),
        (
            names: [
                "-f",
                "--format",
            ],
            description: Some("Output format. This can either be a specifier string (similar to strftime()\'s FMT) or the name of a calendar"),
            takes_value: true,
            arguments: [
                (
                    name: "string",
                    source: Files,
                    optional: false,
                    variadic: false,
                    imported_generator: None,
                ),
            ],
        ),
        (
            names: [
                "-i",
                "--input-format",
            ],
            description: Some("Input format, can be used multiple times. Each date/time will be passed to the input format parsers in the order they are given, if a date/time can be read successfully with a given input format specifier string, that value will be used"),
            takes_value: true,
            arguments: [
                (
                    name: "input format",
                    source: Files,
                    optional: false,
                    variadic: false,
                    imported_generator: None,
                ),
            ],
        ),
        (
            names: [
                "-b",
                "--base",
            ],
            description: Some("For underspecified input use DT as a fallback to fill in missing fields. Also used for ambiguous format specifiers to position their range on the absolute time line. Must be a date/time in ISO8601 format. If omitted defaults to the current date/time"),
            takes_value: true,
            arguments: [
                (
                    name: "base",
                    source: Files,
                    optional: false,
                    variadic: false,
                    imported_generator: None,
                ),
            ],
        ),
        (
            names: [
                "-e",
                "--backslash-escapes",
            ],
            description: Some("Enable interpretation of backslash escapes in the output and input format specifier strings"),
            takes_value: false,
            arguments: [],
        ),
        (
            names: [
                "--locale",
            ],
            description: Some("Format results according to LOCALE, this would only affect month and weekday names"),
            takes_value: true,
            arguments: [
                (
                    name: "locale",
                    source: Files,
                    optional: false,
                    variadic: false,
                    imported_generator: None,
                ),
            ],
        ),
        (
            names: [
                "-s",
                "--skip",
            ],
            description: Some("Skip weekdays specified by WEEKDAY. WEEKDAY can be a single weekday (Mon, Tue, etc.), and to skip several days the --skip option can be used multiple times. WEEKDAY can also be a comma-separated list of weekday names, or `ss\' to skip weekends (sat+sun) altogether. WEEKDAY can also contain date ranges like `mo-we\' for Monday to Wednesday"),
            takes_value: true,
            arguments: [
                (
                    name: "weekday",
                    source: Files,
                    optional: false,
                    variadic: true,
                    imported_generator: None,
                ),
            ],
        ),
        (
            names: [
                "--alt-inc",
            ],
            description: Some("Alternative increment to use when a date is hit that is skipped as per --skip. This increment will be applied until a non-skipped date is reached. The special case `0\' (default) deactivates alternative incrementing.  A useful value could be `1d\' for increasing sequences and `-1d\' for decreasing sequences, so if a skipped date is encountered the next non-skipped date after/before will be used"),
            takes_value: true,
            arguments: [
                (
                    name: "alternative increment",
                    source: Files,
                    optional: false,
                    variadic: false,
                    imported_generator: None,
                ),
            ],
        ),
        (
            names: [
                "--compute-from-last",
            ],
            description: Some("Compute a start value from LAST using INCREMENT. This option has an effect only when INCREMENT is not a divisor of the duration between FIRST and LAST. In such case, an alternative FIRST will be computed by consecutively subtracting INCREMENT from LAST until FIRST is hit or crossed"),
            takes_value: false,
            arguments: [],
        ),
    ],
    arguments: [
        (
            name: "FIRST",
            source: Files,
            optional: false,
            variadic: false,
            imported_generator: None,
        ),
        (
            name: "INCREMENT",
            source: Files,
            optional: true,
            variadic: false,
            imported_generator: None,
        ),
        (
            name: "LAST",
            source: Files,
            optional: false,
            variadic: false,
            imported_generator: None,
        ),
    ],
)