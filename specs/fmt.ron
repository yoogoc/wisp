(
    version: 1,
    command: "fmt",
    description: Some("Simple text formatter"),
    options: [
        (
            names: [
                "-c",
            ],
            description: Some("Center the text, line by line.  In this case, most of the other\noptions are ignored; no splitting or joining of lines is done"),
        ),
        (
            names: [
                "-m",
            ],
            description: Some("Try to format mail header lines contained in the input\nsensibly"),
        ),
        (
            names: [
                "-n",
            ],
            description: Some("Format lines beginning with a ‘.’ (dot) character"),
        ),
        (
            names: [
                "-p",
            ],
            description: Some("Allow indented paragraphs.  Without the -p flag, any change in\nthe amount of whitespace at the start of a line results in a\nnew paragraph being begun"),
        ),
        (
            names: [
                "-s",
            ],
            description: Some("Collapse whitespace inside lines, so that multiple whitespace\ncharacters are turned into a single space.  (Or, at the end of\na sentence, a double space.)"),
        ),
        (
            names: [
                "-d",
            ],
            description: Some("Treat the chars (and no others) as sentence-ending characters.\nBy default the sentence-ending characters are full stop (‘.’),\nquestion mark (‘?’) and exclamation mark (‘!’).  Remember that\nsome characters may need to be escaped to protect them from\nyour shell"),
            arguments: [
                (
                    name: "chars",
                    suggestions: [
                        (
                            name: ".",
                        ),
                        (
                            name: "?",
                        ),
                        (
                            name: "!",
                        ),
                    ],
                    default: Some("."),
                ),
            ],
        ),
        (
            names: [
                "-l",
            ],
            description: Some("Replace multiple spaces with tabs at the start of each output\nline, if possible.  Each number spaces will be replaced with\none tab.  The default is 8.  If number is 0, spaces are\npreserved"),
            arguments: [
                (
                    name: "number",
                    suggestions: [
                        (
                            name: "8",
                        ),
                    ],
                    default: Some("8"),
                ),
            ],
        ),
        (
            names: [
                "-t",
            ],
            description: Some("Assume that the input files' tabs assume number spaces per tab\nstop.  The default is 8"),
            arguments: [
                (
                    name: "number",
                    suggestions: [
                        (
                            name: "8",
                        ),
                    ],
                    default: Some("8"),
                ),
            ],
        ),
    ],
    arguments: [
        (
            name: "file",
            description: Some("File(s) to format"),
            template: [
                FilePaths,
            ],
            optional: true,
            variadic: true,
        ),
    ],
    parser_directives: Some((
        options_must_precede_arguments: true,
    )),
)
