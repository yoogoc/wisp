(
    version: 1,
    command: "split",
    description: Some("The split utility reads the given file and breaks it up into files of 1000 lines each (if no options are specified), leaving the file unchanged. If file is a single dash ('-') or absent, split reads from the standard input"),
    options: [
        (
            names: [
                "-a",
            ],
            description: Some("Use suffix_length letters to form the suffix of the file name"),
            arguments: [
                (
                    name: "suffix_length",
                ),
            ],
        ),
        (
            names: [
                "-b",
            ],
            description: Some("Create split files byte_count bytes in length. If k or K is appended to the number, the file is split into byte_count kilobyte pieces. If m or M is appended to the number, the file is split into byte_count megabyte pieces.  If g or G is appended to the number, the file is split into byte_count gigabyte pieces"),
            arguments: [
                (
                    name: "byte_count",
                    description: Some("N[K|k|M|m|G|g]"),
                ),
            ],
            exclusive_on: [
                "-p",
            ],
        ),
        (
            names: [
                "-d",
            ],
            description: Some("Use a numeric suffix instead of a alphabetic suffix"),
        ),
        (
            names: [
                "-l",
            ],
            description: Some("Create split files line_count lines in length"),
            arguments: [
                (
                    name: "line_count",
                ),
            ],
            exclusive_on: [
                "-p",
            ],
        ),
        (
            names: [
                "-p",
            ],
            description: Some("The file is split whenever an input line matches pattern, which is interpreted as an extended regular expression. The matching line will be the first line of the next output file. This option is incompatible with the -b and -l options"),
            arguments: [
                (
                    name: "pattern",
                ),
            ],
            exclusive_on: [
                "-b",
                "-l",
            ],
        ),
    ],
    arguments: [
        (
            name: "file",
            description: Some("The file to split"),
            template: [
                FilePaths,
            ],
            optional: true,
        ),
        (
            name: "prefix",
            description: Some("Prefix for the names of the files into which the file is split"),
            suggestions: [
                (
                    name: "x",
                ),
            ],
            optional: true,
            default: Some("x"),
        ),
    ],
)
