(
    version: 1,
    command: "build",
    description: None,
    subcommands: [],
    options: [
        (
            names: [
                "-a",
                "--arch",
            ],
            description: Some("Specifies the target architecture. This is a shorthand syntax for setting the Runtime Identifier (RID), where the provided value is combined with the default RID. For example, on a win-x64 machine, specifying --arch x86 sets the RID to win-x86. If you use this option, don\'t use the -r|--runtime option. Available since .NET 6 Preview 7"),
            takes_value: true,
            arguments: [
                (
                    name: "arch",
                    source: Static([
                        "x86",
                        "x64",
                        "arm",
                        "arm64",
                    ]),
                    optional: false,
                    variadic: false,
                    imported_generator: None,
                ),
            ],
        ),
        (
            names: [
                "-c",
                "--configuration",
            ],
            description: Some("Defines the build configuration. The default for most projects is Debug, but you can override the build configuration settings in your project"),
            takes_value: true,
            arguments: [
                (
                    name: "config",
                    source: Files,
                    optional: false,
                    variadic: false,
                    imported_generator: None,
                ),
            ],
        ),
        (
            names: [
                "-f",
                "--framework",
            ],
            description: Some("Compiles for a specific framework. The framework must be defined in the project file"),
            takes_value: false,
            arguments: [],
        ),
        (
            names: [
                "--force",
            ],
            description: Some("Forces all dependencies to be resolved even if the last restore was successful. Specifying this flag is the same as deleting the project.assets.json file"),
            takes_value: false,
            arguments: [],
        ),
        (
            names: [
                "--interactive",
            ],
            description: Some("Allows the command to stop and wait for user input or action. For example, to complete authentication. Available since .NET Core 3.0 SDK"),
            takes_value: false,
            arguments: [],
        ),
        (
            names: [
                "--no-dependencies",
            ],
            description: Some("Ignores project-to-project (P2P) references and only builds the specified root project"),
            takes_value: false,
            arguments: [],
        ),
        (
            names: [
                "--no-incremental",
            ],
            description: Some("Marks the build as unsafe for incremental build. This flag turns off incremental compilation and forces a clean rebuild of the project\'s dependency graph"),
            takes_value: false,
            arguments: [],
        ),
        (
            names: [
                "--no-restore",
            ],
            description: Some("Doesn\'t execute an implicit restore during build"),
            takes_value: false,
            arguments: [],
        ),
        (
            names: [
                "--nologo",
            ],
            description: Some("Doesn\'t display the startup banner or the copyright message. Available since .NET Core 3.0 SDK"),
            takes_value: false,
            arguments: [],
        ),
        (
            names: [
                "--no-self-contained",
            ],
            description: Some("Publishes the application as a framework dependent application. A compatible .NET runtime must be installed on the target machine to run the application"),
            takes_value: false,
            arguments: [],
        ),
        (
            names: [
                "-o",
                "--output",
            ],
            description: Some("Directory in which to place the built binaries. If not specified, the default path is ./bin/<configuration>/<framework>/. For projects with multiple target frameworks (via the TargetFrameworks property), you also need to define --framework when you specify this option"),
            takes_value: true,
            arguments: [
                (
                    name: "directory",
                    source: Directories,
                    optional: false,
                    variadic: false,
                    imported_generator: None,
                ),
            ],
        ),
        (
            names: [
                "--os",
            ],
            description: Some("Specifies the target operating system (OS). This is a shorthand syntax for setting the Runtime Identifier (RID), where the provided value is combined with the default RID. For example, on a win-x64 machine, specifying --os os sets the RID to os-x64. If you use this option, don\'t use the -r|--runtime option. Available since .NET 6 Preview 7"),
            takes_value: true,
            arguments: [
                (
                    name: "os",
                    source: Files,
                    optional: false,
                    variadic: false,
                    imported_generator: None,
                ),
            ],
        ),
        (
            names: [
                "-r",
                "--runtime",
            ],
            description: Some("Specifies the target runtime. For a list of Runtime Identifiers (RIDs), see the RID catalog. If you use this option, use --self-contained or --no-self-contained also"),
            takes_value: true,
            arguments: [
                (
                    name: "runtime",
                    source: Files,
                    optional: false,
                    variadic: false,
                    imported_generator: None,
                ),
            ],
        ),
        (
            names: [
                "--self-contained",
            ],
            description: Some("Publishes the .NET runtime with the application so the runtime doesn\'t need to be installed on the target machine. The default is true if a runtime identifier is specified"),
            takes_value: true,
            arguments: [
                (
                    name: "self-contained",
                    source: Static([
                        "true",
                        "false",
                    ]),
                    optional: true,
                    variadic: false,
                    imported_generator: None,
                ),
            ],
        ),
        (
            names: [
                "--source",
            ],
            description: Some("The URI of the NuGet package source to use during the restore operation"),
            takes_value: true,
            arguments: [
                (
                    name: "source",
                    source: Files,
                    optional: false,
                    variadic: false,
                    imported_generator: None,
                ),
            ],
        ),
        (
            names: [
                "-v",
                "--verbosity",
            ],
            description: Some("Sets the verbosity level of the command. Allowed values are q[uiet], m[inimal], n[ormal], d[etailed], and diag[nostic]. Not supported in every command. See specific command page to determine if this option is available"),
            takes_value: true,
            arguments: [
                (
                    name: "verbosity",
                    source: Static([
                        "quiet",
                        "minimal",
                        "normal",
                        "detailed",
                        "diagnostic",
                    ]),
                    optional: false,
                    variadic: false,
                    imported_generator: None,
                ),
            ],
        ),
        (
            names: [
                "--version-suffix",
            ],
            description: Some("Sets the value of the $(VersionSuffix) property to use when building the project. This only works if the $(Version) property isn\'t set. Then, $(Version) is set to the $(VersionPrefix) combined with the $(VersionSuffix), separated by a dash"),
            takes_value: true,
            arguments: [
                (
                    name: "suffix",
                    source: Files,
                    optional: false,
                    variadic: false,
                    imported_generator: None,
                ),
            ],
        ),
    ],
    arguments: [
        (
            name: "project",
            source: Unavailable,
            optional: true,
            variadic: false,
            imported_generator: Some((
                script: [],
                has_post_process: false,
                has_custom: true,
            )),
        ),
    ],
)