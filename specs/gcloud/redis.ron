(
    version: 1,
    command: "redis",
    description: Some("Manage Cloud Memorystore Redis resources"),
    subcommands: [
        (
            name: "instances",
            description: Some("Manage Cloud Memorystore Redis instances"),
            subcommands: [
                (
                    name: "create",
                    description: Some("Create a Memorystore Redis instance"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--alternative-zone",
                            ],
                            description: Some("A secondary zone for the Redis instance. Only applicable to\nthe standard tier. This protects the instance against zonal failures\nby provisioning it across two zones. If provided, alternative zone\nmust be a different zone from the one provided through `--zone`"),
                            arguments: [
                                (
                                    name: "ALTERNATIVE_ZONE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--async",
                            ],
                            description: Some("Return immediately, without waiting for the operation in progress to\ncomplete"),
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--connect-mode",
                            ],
                            description: Some("Network connection mode used by instances. _CONNECT_MODE_ must be one of: *connect-mode-unspecified*, *direct-peering*, *private-service-access*"),
                            arguments: [
                                (
                                    name: "CONNECT_MODE",
                                    description: Some("Googlecloudsdk.command_lib.util.apis.arg_utils:EnumNameToChoice"),
                                    suggestions: [
                                        (
                                            name: "connect-mode-unspecified",
                                        ),
                                        (
                                            name: "direct-peering",
                                        ),
                                        (
                                            name: "private-service-access",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--display-name",
                            ],
                            description: Some("A human-readable name for the instance"),
                            arguments: [
                                (
                                    name: "DISPLAY_NAME",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--labels",
                            ],
                            description: Some("List of label KEY=VALUE pairs to add.\n+\nKeys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers"),
                            arguments: [
                                (
                                    name: "KEY=VALUE",
                                    description: Some("Dict"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--network",
                            ],
                            description: Some("The name of the Google Compute Engine network to which the instance\nwill be connected. If left unspecified, the default network will be\nused"),
                            arguments: [
                                (
                                    name: "NETWORK",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--redis-config",
                            ],
                            description: Some("A list of Redis config KEY=VALUE pairs to set on the instance\naccording to http://redis.io/topics/config. Currently, the only\nsupported parameters are:\n+\nRedis version 3.2 and newer: maxmemory-policy, notify-keyspace-events.\n+\nRedis version 4.0 and newer: activedefrag, lfu-decay-time, lfu-log-factor, maxmemory-gb.\n+\nRedis version 5.0 and newer: stream-node-max-bytes, stream-node-max-entries"),
                            arguments: [
                                (
                                    name: "KEY=VALUE",
                                    description: Some("Googlecloudsdk.command_lib.util.apis.yaml_command_schema_util:Parse"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--redis-version",
                            ],
                            description: Some("The version of Redis software. _VERSION_ must be one of:\n+\n*redis_3_2*::: Redis 3.2 compatibility\n*redis_4_0*::: Redis 4.0 compatibility\n*redis_5_0*::: Redis 5.0 compatibility\n:::\n+"),
                            arguments: [
                                (
                                    name: "VERSION",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "redis_3_2",
                                        ),
                                        (
                                            name: "redis_4_0",
                                        ),
                                        (
                                            name: "redis_5_0",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--region",
                            ],
                            description: Some("The name of the Redis region of the instance. Overrides the default\nredis/region property value for this command invocation"),
                            arguments: [
                                (
                                    name: "REGION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--reserved-ip-range",
                            ],
                            description: Some("The CIDR range of internal addresses that are reserved for this\ninstance. For example, 10.0.0.0/29 or 192.168.0.0/29. Range must be\nunique and non-overlapping with existing ranges in the network. If left\nunspecified, the service will automatically pick an available range"),
                            arguments: [
                                (
                                    name: "RESERVED_IP_RANGE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--size",
                            ],
                            description: Some("The memory size of the instance in GiB. If not provided, size of 1 GiB\nwill be used"),
                            arguments: [
                                (
                                    name: "SIZE",
                                    description: Some("Int"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--tier",
                            ],
                            description: Some("The service tier of the instance. _TIER_ must be one of:\n+\n*basic*::: Basic Redis instance with no replication\n*standard*::: Standard high-availability Redis instance with replication\n:::\n+"),
                            arguments: [
                                (
                                    name: "TIER",
                                    description: Some("Googlecloudsdk.command_lib.util.apis.arg_utils:EnumNameToChoice"),
                                    suggestions: [
                                        (
                                            name: "basic",
                                        ),
                                        (
                                            name: "standard",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--zone",
                            ],
                            description: Some("The zone of the Redis instance. If not provided the service\nwill pick a random zone in the region. For the standard tier, instances\nwill be created across two zones for protection against zonal\nfailures. So if --alternative-zone is also provided, it must be\ndifferent from --zone"),
                            arguments: [
                                (
                                    name: "ZONE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                    ],
                    arguments: [
                        (
                            name: "INSTANCE",
                            description: Some("ID of the instance or fully qualified identifier for the instance"),
                        ),
                    ],
                ),
                (
                    name: "delete",
                    description: Some("Delete a Redis instance"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--async",
                            ],
                            description: Some("Return immediately, without waiting for the operation in progress to\ncomplete"),
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--region",
                            ],
                            description: Some("The name of the Redis region of the instance. Overrides the default\nredis/region property value for this command invocation"),
                            arguments: [
                                (
                                    name: "REGION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                    ],
                    arguments: [
                        (
                            name: "INSTANCE",
                            description: Some("ID of the instance or fully qualified identifier for the instance"),
                        ),
                    ],
                ),
                (
                    name: "describe",
                    description: Some("Show metadata for a Memorystore Redis instance"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--region",
                            ],
                            description: Some("The name of the Redis region of the instance. Overrides the default\nredis/region property value for this command invocation"),
                            arguments: [
                                (
                                    name: "REGION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                    ],
                    arguments: [
                        (
                            name: "INSTANCE",
                            description: Some("ID of the instance or fully qualified identifier for the instance"),
                        ),
                    ],
                ),
                (
                    name: "export",
                    description: Some("Export data from a Memorystore Redis instance to Google Cloud Storage"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--async",
                            ],
                            description: Some("Return immediately, without waiting for the operation in progress to\ncomplete"),
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--region",
                            ],
                            description: Some("The name of the Redis region of the instance. Overrides the default\nredis/region property value for this command invocation"),
                            arguments: [
                                (
                                    name: "REGION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                    ],
                    arguments: [
                        (
                            name: "DESTINATION",
                            description: Some("The Cloud Storage object path to export the instance to. Must have\nthe redis DB file extension *.rdb*"),
                        ),
                        (
                            name: "INSTANCE",
                            description: Some("ID of the instance or fully qualified identifier for the instance"),
                        ),
                    ],
                ),
                (
                    name: "failover",
                    description: Some("Failover a standard tier Cloud Memorystore for Redis instance from the master node to its replica"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--async",
                            ],
                            description: Some("Return immediately, without waiting for the operation in progress to\ncomplete"),
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--data-protection-mode",
                            ],
                            description: Some("Data protection mode to use for the failover. If not specified, defaults to\n'limited-data-loss'. _DATA_PROTECTION_MODE_ must be one of:\n+\n*force-data-loss*::: Failover without data loss protection. Can cause significant data loss.\n*limited-data-loss*::: Failover with data loss protection that ensures loss is within system thresholds.\n:::\n+"),
                            arguments: [
                                (
                                    name: "DATA_PROTECTION_MODE",
                                    description: Some("Googlecloudsdk.command_lib.util.apis.arg_utils:EnumNameToChoice"),
                                    suggestions: [
                                        (
                                            name: "force-data-loss",
                                        ),
                                        (
                                            name: "limited-data-loss",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--region",
                            ],
                            description: Some("The name of the Redis region of the instance. Overrides the default\nredis/region property value for this command invocation"),
                            arguments: [
                                (
                                    name: "REGION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                    ],
                    arguments: [
                        (
                            name: "INSTANCE",
                            description: Some("ID of the instance or fully qualified identifier for the instance"),
                        ),
                    ],
                ),
                (
                    name: "import",
                    description: Some("Import data to a Memorystore Redis instance from Google Cloud Storage"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--async",
                            ],
                            description: Some("Return immediately, without waiting for the operation in progress to\ncomplete"),
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--region",
                            ],
                            description: Some("The name of the Redis region of the instance. Overrides the default\nredis/region property value for this command invocation"),
                            arguments: [
                                (
                                    name: "REGION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                    ],
                    arguments: [
                        (
                            name: "SOURCE",
                            description: Some("The Cloud Storage object path to import the instance from. Must have\nthe redis DB file extension *.rdb*"),
                        ),
                        (
                            name: "INSTANCE",
                            description: Some("ID of the instance or fully qualified identifier for the instance"),
                        ),
                    ],
                ),
                (
                    name: "list",
                    description: Some("List Memorystore Redis instances"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--filter",
                            ],
                            description: Some("Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.\nIf the expression evaluates `True`, then that item is listed. For more\ndetails and examples of filter expressions, run $ gcloud topic filters. This\nflag interacts with other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "EXPRESSION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--limit",
                            ],
                            description: Some("Maximum number of resources to list. The default is *unlimited*.\nThis flag interacts with other flags that are applied in this order:\n*--flatten*, *--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "LIMIT",
                                    description: Some("Googlecloudsdk.calliope.arg_parsers:Parse"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--page-size",
                            ],
                            description: Some("Some services group resource list output into pages. This flag specifies\nthe maximum number of resources per page. The default is determined by the\nservice if it supports paging, otherwise it is *unlimited* (no paging).\nPaging may be applied before or after *--filter* and *--limit* depending\non the service"),
                            arguments: [
                                (
                                    name: "PAGE_SIZE",
                                    description: Some("Googlecloudsdk.calliope.arg_parsers:Parse"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--region",
                            ],
                            description: Some("ID of the region or fully qualified identifier for the region"),
                            arguments: [
                                (
                                    name: "REGION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--sort-by",
                            ],
                            description: Some("Comma-separated list of resource field key names to sort by. The\ndefault order is ascending. Prefix a field with ``~'' for descending\norder on that field. This flag interacts with other flags that are applied\nin this order: *--flatten*, *--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "FIELD",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--uri",
                            ],
                            description: Some("Print a list of resource URIs instead of the default output"),
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                    ],
                ),
                (
                    name: "update",
                    description: Some("Update Memorystore Redis instances"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--async",
                            ],
                            description: Some("Return immediately, without waiting for the operation in progress to\ncomplete"),
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--clear-labels",
                            ],
                            description: Some("Remove all labels. If `--update-labels` is also specified then\n`--clear-labels` is applied first.\n+\nFor example, to remove all labels:\n+\n    $ {command} --clear-labels\n+\nTo set the labels to exactly \"foo\" and \"baz\":\n+\n    $ {command} --clear-labels --update-labels foo=bar,baz=qux"),
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--display-name",
                            ],
                            description: Some("A human-readable name for the instance"),
                            arguments: [
                                (
                                    name: "DISPLAY_NAME",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--region",
                            ],
                            description: Some("The name of the Redis region of the instance. Overrides the default\nredis/region property value for this command invocation"),
                            arguments: [
                                (
                                    name: "REGION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--remove-labels",
                            ],
                            description: Some("List of label keys to remove. If a label does not exist it is\nsilently ignored. If `--update-labels` is also specified then\n`--remove-labels` is applied first"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--remove-redis-config",
                            ],
                            description: Some("A list of Redis config parameters to remove. Removing a non-existent\nconfig parameter is silently ignored"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--size",
                            ],
                            description: Some("The memory size of the instance in GiB"),
                            arguments: [
                                (
                                    name: "SIZE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--update-labels",
                            ],
                            description: Some("List of label KEY=VALUE pairs to update. If a label exists its value is modified, otherwise a new label is created.\n+\nKeys must start with a lowercase character and contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers. Values must contain only hyphens (`-`), underscores (```_```), lowercase characters, and numbers"),
                            arguments: [
                                (
                                    name: "KEY=VALUE",
                                    description: Some("Dict"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--update-redis-config",
                            ],
                            description: Some("A list of Redis config KEY=VALUE pairs to update according to\nhttp://cloud.google.com/memorystore/docs/reference/redis-configs. If a config parameter is already set,\nits value is modified; otherwise a new Redis config parameter is added.\nCurrently, the only supported parameters are:\n+\nRedis version 3.2 and newer: maxmemory-policy, notify-keyspace-events.\n+\nRedis version 4.0 and newer: activedefrag, lfu-decay-time, lfu-log-factor, maxmemory-gb.\n+\nRedis version 5.0 and newer: stream-node-max-bytes, stream-node-max-entries"),
                            arguments: [
                                (
                                    name: "KEY=VALUE",
                                    description: Some("Googlecloudsdk.command_lib.redis.util:InstanceRedisConfigArgType"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                    ],
                    arguments: [
                        (
                            name: "INSTANCE",
                            description: Some("ID of the instance or fully qualified identifier for the instance"),
                        ),
                    ],
                ),
                (
                    name: "upgrade",
                    description: Some("Upgrade a Memorystore for Redis instance to a specified Redis version"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--async",
                            ],
                            description: Some("Return immediately, without waiting for the operation in progress to\ncomplete"),
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--redis-version",
                            ],
                            description: Some("Target version of Redis software. You can only upgrade to the next highest version. For example, you must upgrade an instance from redis_3_2 to redis_4_0 before you can upgrade the instance to redis_5_0. _VERSION_ must be one of:\n+\n*redis_4_0*::: Redis 4.0 compatibility\n*redis_5_0*::: Redis 5.0 compatibility\n:::\n+"),
                            arguments: [
                                (
                                    name: "VERSION",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "redis_4_0",
                                        ),
                                        (
                                            name: "redis_5_0",
                                        ),
                                    ],
                                ),
                            ],
                            presentation: (
                                priority: Some(100.0),
                            ),
                        ),
                        (
                            names: [
                                "--region",
                            ],
                            description: Some("The name of the Redis region of the instance. Overrides the default\nredis/region property value for this command invocation"),
                            arguments: [
                                (
                                    name: "REGION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                    ],
                    arguments: [
                        (
                            name: "INSTANCE",
                            description: Some("ID of the instance or fully qualified identifier for the instance"),
                        ),
                    ],
                ),
            ],
            options: [
                (
                    names: [
                        "--account",
                    ],
                    description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                    arguments: [
                        (
                            name: "ACCOUNT",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--billing-project",
                    ],
                    description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                    arguments: [
                        (
                            name: "BILLING_PROJECT",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--configuration",
                    ],
                    description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                    arguments: [
                        (
                            name: "CONFIGURATION",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--flags-file",
                    ],
                    description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                    arguments: [
                        (
                            name: "YAML_FILE",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--flatten",
                    ],
                    description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                    arguments: [
                        (
                            name: "KEY",
                            description: Some("List"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--format",
                    ],
                    description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                    arguments: [
                        (
                            name: "FORMAT",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--help",
                    ],
                    description: Some("Display detailed help"),
                ),
                (
                    names: [
                        "--impersonate-service-account",
                    ],
                    description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                    arguments: [
                        (
                            name: "SERVICE_ACCOUNT_EMAIL",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--log-http",
                    ],
                    description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                ),
                (
                    names: [
                        "--project",
                    ],
                    description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                    arguments: [
                        (
                            name: "PROJECT_ID",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--quiet",
                    ],
                    description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                ),
                (
                    names: [
                        "--trace-token",
                    ],
                    description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                    arguments: [
                        (
                            name: "TRACE_TOKEN",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--user-output-enabled",
                    ],
                    description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                ),
                (
                    names: [
                        "--verbosity",
                    ],
                    description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                    arguments: [
                        (
                            name: "VERBOSITY",
                            description: Some("String"),
                            suggestions: [
                                (
                                    name: "debug",
                                ),
                                (
                                    name: "info",
                                ),
                                (
                                    name: "warning",
                                ),
                                (
                                    name: "error",
                                ),
                                (
                                    name: "critical",
                                ),
                                (
                                    name: "none",
                                ),
                            ],
                        ),
                    ],
                ),
            ],
        ),
        (
            name: "operations",
            description: Some("Manage Cloud Memorystore Redis operations"),
            subcommands: [
                (
                    name: "cancel",
                    description: Some("Cancel a Memorystore Redis import or export operation"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--region",
                            ],
                            description: Some("The name of the Redis region of the operation. Overrides the default\nredis/region property value for this command invocation"),
                            arguments: [
                                (
                                    name: "REGION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                    ],
                    arguments: [
                        (
                            name: "OPERATION",
                            description: Some("ID of the operation or fully qualified identifier for the operation"),
                        ),
                    ],
                ),
                (
                    name: "describe",
                    description: Some("Show metadata for a Memorystore Redis operation"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--region",
                            ],
                            description: Some("The name of the Redis region of the operation. Overrides the default\nredis/region property value for this command invocation"),
                            arguments: [
                                (
                                    name: "REGION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                    ],
                    arguments: [
                        (
                            name: "OPERATION",
                            description: Some("ID of the operation or fully qualified identifier for the operation"),
                        ),
                    ],
                ),
                (
                    name: "list",
                    description: Some("List Memorystore Redis operations"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--filter",
                            ],
                            description: Some("Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.\nIf the expression evaluates `True`, then that item is listed. For more\ndetails and examples of filter expressions, run $ gcloud topic filters. This\nflag interacts with other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "EXPRESSION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--limit",
                            ],
                            description: Some("Maximum number of resources to list. The default is *unlimited*.\nThis flag interacts with other flags that are applied in this order:\n*--flatten*, *--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "LIMIT",
                                    description: Some("Googlecloudsdk.calliope.arg_parsers:Parse"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--page-size",
                            ],
                            description: Some("Some services group resource list output into pages. This flag specifies\nthe maximum number of resources per page. The default is determined by the\nservice if it supports paging, otherwise it is *unlimited* (no paging).\nPaging may be applied before or after *--filter* and *--limit* depending\non the service"),
                            arguments: [
                                (
                                    name: "PAGE_SIZE",
                                    description: Some("Googlecloudsdk.calliope.arg_parsers:Parse"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--region",
                            ],
                            description: Some("ID of the region or fully qualified identifier for the region"),
                            arguments: [
                                (
                                    name: "REGION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--sort-by",
                            ],
                            description: Some("Comma-separated list of resource field key names to sort by. The\ndefault order is ascending. Prefix a field with ``~'' for descending\norder on that field. This flag interacts with other flags that are applied\nin this order: *--flatten*, *--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "FIELD",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--uri",
                            ],
                            description: Some("Print a list of resource URIs instead of the default output"),
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                    ],
                ),
            ],
            options: [
                (
                    names: [
                        "--account",
                    ],
                    description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                    arguments: [
                        (
                            name: "ACCOUNT",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--billing-project",
                    ],
                    description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                    arguments: [
                        (
                            name: "BILLING_PROJECT",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--configuration",
                    ],
                    description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                    arguments: [
                        (
                            name: "CONFIGURATION",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--flags-file",
                    ],
                    description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                    arguments: [
                        (
                            name: "YAML_FILE",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--flatten",
                    ],
                    description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                    arguments: [
                        (
                            name: "KEY",
                            description: Some("List"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--format",
                    ],
                    description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                    arguments: [
                        (
                            name: "FORMAT",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--help",
                    ],
                    description: Some("Display detailed help"),
                ),
                (
                    names: [
                        "--impersonate-service-account",
                    ],
                    description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                    arguments: [
                        (
                            name: "SERVICE_ACCOUNT_EMAIL",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--log-http",
                    ],
                    description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                ),
                (
                    names: [
                        "--project",
                    ],
                    description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                    arguments: [
                        (
                            name: "PROJECT_ID",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--quiet",
                    ],
                    description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                ),
                (
                    names: [
                        "--trace-token",
                    ],
                    description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                    arguments: [
                        (
                            name: "TRACE_TOKEN",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--user-output-enabled",
                    ],
                    description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                ),
                (
                    names: [
                        "--verbosity",
                    ],
                    description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                    arguments: [
                        (
                            name: "VERBOSITY",
                            description: Some("String"),
                            suggestions: [
                                (
                                    name: "debug",
                                ),
                                (
                                    name: "info",
                                ),
                                (
                                    name: "warning",
                                ),
                                (
                                    name: "error",
                                ),
                                (
                                    name: "critical",
                                ),
                                (
                                    name: "none",
                                ),
                            ],
                        ),
                    ],
                ),
            ],
        ),
        (
            name: "regions",
            description: Some("Manage Cloud Memorystore Redis regions"),
            subcommands: [
                (
                    name: "describe",
                    description: Some("Show metadata for a Memorystore Redis region"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                    ],
                    arguments: [
                        (
                            name: "REGION",
                            description: Some("ID of the region or fully qualified identifier for the region"),
                        ),
                    ],
                ),
                (
                    name: "list",
                    description: Some("List Memorystore Redis regions"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--filter",
                            ],
                            description: Some("Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.\nIf the expression evaluates `True`, then that item is listed. For more\ndetails and examples of filter expressions, run $ gcloud topic filters. This\nflag interacts with other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "EXPRESSION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--limit",
                            ],
                            description: Some("Maximum number of resources to list. The default is *unlimited*.\nThis flag interacts with other flags that are applied in this order:\n*--flatten*, *--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "LIMIT",
                                    description: Some("Googlecloudsdk.calliope.arg_parsers:Parse"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--page-size",
                            ],
                            description: Some("Some services group resource list output into pages. This flag specifies\nthe maximum number of resources per page. The default is determined by the\nservice if it supports paging, otherwise it is *unlimited* (no paging).\nPaging may be applied before or after *--filter* and *--limit* depending\non the service"),
                            arguments: [
                                (
                                    name: "PAGE_SIZE",
                                    description: Some("Googlecloudsdk.calliope.arg_parsers:Parse"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--sort-by",
                            ],
                            description: Some("Comma-separated list of resource field key names to sort by. The\ndefault order is ascending. Prefix a field with ``~'' for descending\norder on that field. This flag interacts with other flags that are applied\nin this order: *--flatten*, *--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "FIELD",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--uri",
                            ],
                            description: Some("Print a list of resource URIs instead of the default output"),
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                    ],
                ),
            ],
            options: [
                (
                    names: [
                        "--account",
                    ],
                    description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                    arguments: [
                        (
                            name: "ACCOUNT",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--billing-project",
                    ],
                    description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                    arguments: [
                        (
                            name: "BILLING_PROJECT",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--configuration",
                    ],
                    description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                    arguments: [
                        (
                            name: "CONFIGURATION",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--flags-file",
                    ],
                    description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                    arguments: [
                        (
                            name: "YAML_FILE",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--flatten",
                    ],
                    description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                    arguments: [
                        (
                            name: "KEY",
                            description: Some("List"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--format",
                    ],
                    description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                    arguments: [
                        (
                            name: "FORMAT",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--help",
                    ],
                    description: Some("Display detailed help"),
                ),
                (
                    names: [
                        "--impersonate-service-account",
                    ],
                    description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                    arguments: [
                        (
                            name: "SERVICE_ACCOUNT_EMAIL",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--log-http",
                    ],
                    description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                ),
                (
                    names: [
                        "--project",
                    ],
                    description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                    arguments: [
                        (
                            name: "PROJECT_ID",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--quiet",
                    ],
                    description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                ),
                (
                    names: [
                        "--trace-token",
                    ],
                    description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                    arguments: [
                        (
                            name: "TRACE_TOKEN",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--user-output-enabled",
                    ],
                    description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                ),
                (
                    names: [
                        "--verbosity",
                    ],
                    description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                    arguments: [
                        (
                            name: "VERBOSITY",
                            description: Some("String"),
                            suggestions: [
                                (
                                    name: "debug",
                                ),
                                (
                                    name: "info",
                                ),
                                (
                                    name: "warning",
                                ),
                                (
                                    name: "error",
                                ),
                                (
                                    name: "critical",
                                ),
                                (
                                    name: "none",
                                ),
                            ],
                        ),
                    ],
                ),
            ],
        ),
        (
            name: "zones",
            description: Some("Manage Cloud Memorystore Redis zones"),
            subcommands: [
                (
                    name: "list",
                    description: Some("List Memorystore Redis zones"),
                    options: [
                        (
                            names: [
                                "--account",
                            ],
                            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "ACCOUNT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--billing-project",
                            ],
                            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                            arguments: [
                                (
                                    name: "BILLING_PROJECT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--configuration",
                            ],
                            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                            arguments: [
                                (
                                    name: "CONFIGURATION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--filter",
                            ],
                            description: Some("Apply a Boolean filter _EXPRESSION_ to each resource item to be listed.\nIf the expression evaluates `True`, then that item is listed. For more\ndetails and examples of filter expressions, run $ gcloud topic filters. This\nflag interacts with other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "EXPRESSION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flags-file",
                            ],
                            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                            arguments: [
                                (
                                    name: "YAML_FILE",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--flatten",
                            ],
                            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "KEY",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--format",
                            ],
                            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                            arguments: [
                                (
                                    name: "FORMAT",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--help",
                            ],
                            description: Some("Display detailed help"),
                        ),
                        (
                            names: [
                                "--impersonate-service-account",
                            ],
                            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "SERVICE_ACCOUNT_EMAIL",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--limit",
                            ],
                            description: Some("Maximum number of resources to list. The default is *unlimited*.\nThis flag interacts with other flags that are applied in this order:\n*--flatten*, *--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "LIMIT",
                                    description: Some("Googlecloudsdk.calliope.arg_parsers:Parse"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--log-http",
                            ],
                            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                        ),
                        (
                            names: [
                                "--page-size",
                            ],
                            description: Some("Some services group resource list output into pages. This flag specifies\nthe maximum number of resources per page. The default is determined by the\nservice if it supports paging, otherwise it is *unlimited* (no paging).\nPaging may be applied before or after *--filter* and *--limit* depending\non the service"),
                            arguments: [
                                (
                                    name: "PAGE_SIZE",
                                    description: Some("Googlecloudsdk.calliope.arg_parsers:Parse"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--project",
                            ],
                            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                            arguments: [
                                (
                                    name: "PROJECT_ID",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--quiet",
                            ],
                            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                        ),
                        (
                            names: [
                                "--region",
                            ],
                            description: Some("If provided, returns only resources from the given region. Use region\nID only, not the full URI of the region. This flag is the equivalent\nof: `--filter=\"region:REGION\"`. Run `gcloud topic filters` for more\ninformation"),
                            arguments: [
                                (
                                    name: "REGION",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--sort-by",
                            ],
                            description: Some("Comma-separated list of resource field key names to sort by. The\ndefault order is ascending. Prefix a field with ``~'' for descending\norder on that field. This flag interacts with other flags that are applied\nin this order: *--flatten*, *--sort-by*, *--filter*, *--limit*"),
                            arguments: [
                                (
                                    name: "FIELD",
                                    description: Some("List"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--trace-token",
                            ],
                            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                            arguments: [
                                (
                                    name: "TRACE_TOKEN",
                                    description: Some("String"),
                                ),
                            ],
                        ),
                        (
                            names: [
                                "--user-output-enabled",
                            ],
                            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                        ),
                        (
                            names: [
                                "--verbosity",
                            ],
                            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                            arguments: [
                                (
                                    name: "VERBOSITY",
                                    description: Some("String"),
                                    suggestions: [
                                        (
                                            name: "debug",
                                        ),
                                        (
                                            name: "info",
                                        ),
                                        (
                                            name: "warning",
                                        ),
                                        (
                                            name: "error",
                                        ),
                                        (
                                            name: "critical",
                                        ),
                                        (
                                            name: "none",
                                        ),
                                    ],
                                ),
                            ],
                        ),
                    ],
                ),
            ],
            options: [
                (
                    names: [
                        "--account",
                    ],
                    description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
                    arguments: [
                        (
                            name: "ACCOUNT",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--billing-project",
                    ],
                    description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
                    arguments: [
                        (
                            name: "BILLING_PROJECT",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--configuration",
                    ],
                    description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
                    arguments: [
                        (
                            name: "CONFIGURATION",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--flags-file",
                    ],
                    description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
                    arguments: [
                        (
                            name: "YAML_FILE",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--flatten",
                    ],
                    description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
                    arguments: [
                        (
                            name: "KEY",
                            description: Some("List"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--format",
                    ],
                    description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
                    arguments: [
                        (
                            name: "FORMAT",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--help",
                    ],
                    description: Some("Display detailed help"),
                ),
                (
                    names: [
                        "--impersonate-service-account",
                    ],
                    description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
                    arguments: [
                        (
                            name: "SERVICE_ACCOUNT_EMAIL",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--log-http",
                    ],
                    description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
                ),
                (
                    names: [
                        "--project",
                    ],
                    description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
                    arguments: [
                        (
                            name: "PROJECT_ID",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--quiet",
                    ],
                    description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
                ),
                (
                    names: [
                        "--trace-token",
                    ],
                    description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
                    arguments: [
                        (
                            name: "TRACE_TOKEN",
                            description: Some("String"),
                        ),
                    ],
                ),
                (
                    names: [
                        "--user-output-enabled",
                    ],
                    description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
                ),
                (
                    names: [
                        "--verbosity",
                    ],
                    description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
                    arguments: [
                        (
                            name: "VERBOSITY",
                            description: Some("String"),
                            suggestions: [
                                (
                                    name: "debug",
                                ),
                                (
                                    name: "info",
                                ),
                                (
                                    name: "warning",
                                ),
                                (
                                    name: "error",
                                ),
                                (
                                    name: "critical",
                                ),
                                (
                                    name: "none",
                                ),
                            ],
                        ),
                    ],
                ),
            ],
        ),
    ],
    options: [
        (
            names: [
                "--account",
            ],
            description: Some("Google Cloud Platform user account to use for invocation. Overrides the default *core/account* property value for this command invocation"),
            arguments: [
                (
                    name: "ACCOUNT",
                    description: Some("String"),
                ),
            ],
        ),
        (
            names: [
                "--billing-project",
            ],
            description: Some("The Google Cloud Platform project that will be charged quota for operations performed in gcloud. If you need to operate on one project, but need quota against a different project, you can use this flag to specify the billing project. If both `billing/quota_project` and `--billing-project` are specified, `--billing-project` takes precedence. Run `$ gcloud config set --help` to see more information about `billing/quota_project`"),
            arguments: [
                (
                    name: "BILLING_PROJECT",
                    description: Some("String"),
                ),
            ],
        ),
        (
            names: [
                "--configuration",
            ],
            description: Some("The configuration to use for this command invocation. For more\ninformation on how to use configurations, run:\n`gcloud topic configurations`.  You can also use the CLOUDSDK_ACTIVE_CONFIG_NAME environment\nvariable to set the equivalent of this flag for a terminal\nsession"),
            arguments: [
                (
                    name: "CONFIGURATION",
                    description: Some("String"),
                ),
            ],
        ),
        (
            names: [
                "--flags-file",
            ],
            description: Some("A YAML or JSON file that specifies a *--flag*:*value* dictionary.\nUseful for specifying complex flag values with special characters\nthat work with any command interpreter. Additionally, each\n*--flags-file* arg is replaced by its constituent flags. See\n$ gcloud topic flags-file for more information"),
            arguments: [
                (
                    name: "YAML_FILE",
                    description: Some("String"),
                ),
            ],
        ),
        (
            names: [
                "--flatten",
            ],
            description: Some("Flatten _name_[] output resource slices in _KEY_ into separate records\nfor each item in each slice. Multiple keys and slices may be specified.\nThis also flattens keys for *--format* and *--filter*. For example,\n*--flatten=abc.def* flattens *abc.def[].ghi* references to\n*abc.def.ghi*. A resource record containing *abc.def[]* with N elements\nwill expand to N records in the flattened output. This flag interacts\nwith other flags that are applied in this order: *--flatten*,\n*--sort-by*, *--filter*, *--limit*"),
            arguments: [
                (
                    name: "KEY",
                    description: Some("List"),
                ),
            ],
        ),
        (
            names: [
                "--format",
            ],
            description: Some("Set the format for printing command output resources. The default is a\ncommand-specific human-friendly output format. The supported formats\nare: `config`, `csv`, `default`, `diff`, `disable`, `flattened`, `get`, `json`, `list`, `multi`, `none`, `object`, `table`, `text`, `value`, `yaml`. For more details run $ gcloud topic formats"),
            arguments: [
                (
                    name: "FORMAT",
                    description: Some("String"),
                ),
            ],
        ),
        (
            names: [
                "--help",
            ],
            description: Some("Display detailed help"),
        ),
        (
            names: [
                "--impersonate-service-account",
            ],
            description: Some("For this gcloud invocation, all API requests will be made as the given service account instead of the currently selected account. This is done without needing to create, download, and activate a key for the account. In order to perform operations as the service account, your currently selected account must have an IAM role that includes the iam.serviceAccounts.getAccessToken permission for the service account. The roles/iam.serviceAccountTokenCreator role has this permission or you may create a custom role. Overrides the default *auth/impersonate_service_account* property value for this command invocation"),
            arguments: [
                (
                    name: "SERVICE_ACCOUNT_EMAIL",
                    description: Some("String"),
                ),
            ],
        ),
        (
            names: [
                "--log-http",
            ],
            description: Some("Log all HTTP server requests and responses to stderr. Overrides the default *core/log_http* property value for this command invocation"),
        ),
        (
            names: [
                "--project",
            ],
            description: Some("The Google Cloud Platform project ID to use for this invocation. If\nomitted, then the current project is assumed; the current project can\nbe listed using `gcloud config list --format='text(core.project)'`\nand can be set using `gcloud config set project PROJECTID`.\n+\n`--project` and its fallback `core/project` property play two roles\nin the invocation. It specifies the project of the resource to\noperate on. It also specifies the project for API enablement check,\nquota, and billing. To specify a different project for quota and\nbilling, use `--billing-project` or `billing/quota_project` property"),
            arguments: [
                (
                    name: "PROJECT_ID",
                    description: Some("String"),
                ),
            ],
        ),
        (
            names: [
                "--quiet",
            ],
            description: Some("Disable all interactive prompts when running gcloud commands. If input\nis required, defaults will be used, or an error will be raised.\nOverrides the default core/disable_prompts property value for this\ncommand invocation. This is equivalent to setting the environment\nvariable `CLOUDSDK_CORE_DISABLE_PROMPTS` to 1"),
        ),
        (
            names: [
                "--trace-token",
            ],
            description: Some("Token used to route traces of service requests for investigation of issues. Overrides the default *core/trace_token* property value for this command invocation"),
            arguments: [
                (
                    name: "TRACE_TOKEN",
                    description: Some("String"),
                ),
            ],
        ),
        (
            names: [
                "--user-output-enabled",
            ],
            description: Some("Print user intended output to the console. Overrides the default *core/user_output_enabled* property value for this command invocation. Use *--no-user-output-enabled* to disable"),
        ),
        (
            names: [
                "--verbosity",
            ],
            description: Some("Override the default verbosity for this command. Overrides the default *core/verbosity* property value for this command invocation. _VERBOSITY_ must be one of: *debug*, *info*, *warning*, *error*, *critical*, *none*"),
            arguments: [
                (
                    name: "VERBOSITY",
                    description: Some("String"),
                    suggestions: [
                        (
                            name: "debug",
                        ),
                        (
                            name: "info",
                        ),
                        (
                            name: "warning",
                        ),
                        (
                            name: "error",
                        ),
                        (
                            name: "critical",
                        ),
                        (
                            name: "none",
                        ),
                    ],
                ),
            ],
        ),
    ],
)
