(
    version: 1,
    command: "file",
    description: Some("Determine file type"),
    options: [
        (
            names: [
                "--help",
            ],
            description: Some("Print a help message and exit"),
        ),
        (
            names: [
                "--apple",
            ],
            description: Some("Causes the file command to output the file type and creator code\nas used by older MacOS versions.  The code consists of eight\nletters, the first describing the file type, the latter the\ncreator.  This option works properly only for file formats that\nhave the apple-style output defined"),
        ),
        (
            names: [
                "--brief",
                "-b",
            ],
            description: Some("Do not prepend filenames to output lines (brief mode)"),
        ),
        (
            names: [
                "--checking-printoug",
                "-c",
            ],
            description: Some("Cause a checking printout of the parsed form of the magic file.\nThis is usually used in conjunction with the -m option to debug a\nnew magic file before installing it"),
        ),
        (
            names: [
                "--compile",
                "-C",
            ],
            description: Some("Write a magic.mgc output file that contains a pre-parsed version\nof the magic file or directory"),
        ),
        (
            names: [
                "-d",
            ],
            description: Some("Apply the default system tests; this is the default behavior\nunless -M is specified"),
        ),
        (
            names: [
                "-D",
            ],
            description: Some("Print debugging messages"),
        ),
        (
            names: [
                "-E",
            ],
            description: Some("On filesystem errors (file not found etc), instead of handling\nthe error as regular output as POSIX mandates and keep going,\nissue an error message and exit"),
        ),
        (
            names: [
                "--exclude",
                "-e",
            ],
            description: Some("Exclude the test named in testname from the list of tests made to\ndetermine the file type"),
            arguments: [
                (
                    name: "testname",
                    suggestions: [
                        (
                            name: "apptype",
                        ),
                        (
                            name: "ascii",
                        ),
                        (
                            name: "encoding",
                        ),
                        (
                            name: "token",
                        ),
                        (
                            name: "cdf",
                        ),
                        (
                            name: "compress",
                        ),
                        (
                            name: "csv",
                        ),
                        (
                            name: "elf",
                        ),
                        (
                            name: "json",
                        ),
                        (
                            name: "soft",
                        ),
                        (
                            name: "tar",
                        ),
                    ],
                ),
            ],
        ),
        (
            names: [
                "--exclude-quiet",
            ],
            description: Some("Like --exclude but ignore tests that file does not know about.\nThis is intended for compatibility with older versions of file"),
        ),
        (
            names: [
                "--extension",
            ],
            description: Some("Print a slash-separated list of valid extensions for the file\ntype found"),
        ),
        (
            names: [
                "--separator",
                "-F",
            ],
            description: Some("Use the specified string as the separator between the filename\nand the file result returned"),
            arguments: [
                (
                    name: "separator",
                    suggestions: [
                        (
                            name: ".",
                        ),
                    ],
                    default: Some("."),
                ),
            ],
        ),
        (
            names: [
                "--files-from",
                "-f",
            ],
            description: Some("Read the names of the files to be examined from namefile (one per\nline) before the argument list.  Either namefile or at least one\nfilename argument must be present; to test the standard input,\nuse ‘-’ as a filename argument.  Please note that namefile is\nunwrapped and the enclosed filenames are processed when this\noption is encountered and before any further options processing\nis done.  This allows one to process multiple lists of files with\ndifferent command line arguments on the same file invocation.\nThus if you want to set the delimiter, you need to do it before\nyou specify the list of files, like: \"-F @ -f namefile\", instead\nof: \"-f namefile -F @\""),
            arguments: [
                (
                    name: "namefile",
                ),
            ],
        ),
        (
            names: [
                "--no-dereference",
                "-h",
            ],
            description: Some("This option causes symlinks not to be followed (on systems that\nsupport symbolic links)"),
        ),
        (
            names: [
                "-i",
            ],
            description: Some("If the file is a regular file, do not classify its contents"),
        ),
        (
            names: [
                "--mime",
                "-I",
            ],
            description: Some("Causes the file command to output mime type strings rather than\nthe more traditional human readable ones.  Thus it may say\n'text/plain; charset=us-ascii' rather than \"ASCII text\""),
        ),
        (
            names: [
                "--mime-type",
                "--mime-encoding",
            ],
            description: Some("Like -I, but print only the specified element(s)"),
        ),
        (
            names: [
                "--keep-going",
                "-k",
            ],
            description: Some("Don't stop at the first match, keep going.  Subsequent matches\nwill be have the string '\\012- ' prepended.  (If you want a\nnewline, see the -r option.)  The magic pattern with the highest\nstrength (see the -l option) comes first"),
        ),
        (
            names: [
                "--list",
                "-l",
            ],
            description: Some("Shows a list of patterns and their strength sorted descending by\nmagic(5) strength which is used for the matching (see also the -k\noption)"),
        ),
        (
            names: [
                "--dereference",
                "-L",
            ],
            description: Some("This option causes symlinks to be followed, as the like-named\noption in ls(1) (on systems that support symbolic links).  This\nis the default behavior"),
        ),
        (
            names: [
                "--magic-file",
                "-m",
            ],
            description: Some("Specify an alternate list of files and directories containing\nmagic.  This can be a single item, or a colon-separated list.  If\na compiled magic file is found alongside a file or directory, it\nwill be used instead"),
            arguments: [
                (
                    name: "list",
                    template: [
                        FilePaths,
                    ],
                ),
            ],
        ),
        (
            names: [
                "-M",
            ],
            description: Some("Like -m, except that the default rules are not applied unless -d\nis specified"),
            arguments: [
                (
                    name: "list",
                    template: [
                        FilePaths,
                    ],
                ),
            ],
        ),
        (
            names: [
                "--no-buffer",
                "-n",
            ],
            description: Some("Force stdout to be flushed after checking each file.  This is\nonly useful if checking a list of files.  It is intended to be\nused by programs that want filetype output from a pipe"),
        ),
        (
            names: [
                "--preserve-data",
                "-p",
            ],
            description: Some("On systems that support utime(3) or utimes(2), attempt to\npreserve the access time of files analyzed, to pretend that file\nnever read them"),
        ),
        (
            names: [
                "--parameter",
                "-P",
            ],
            description: Some("Set various parameter limits"),
            arguments: [
                (
                    name: "name=value",
                    generators: [
                        (
                            has_custom: true,
                            has_dynamic_trigger: true,
                            has_dynamic_query_term: true,
                        ),
                    ],
                ),
            ],
            repeat: Many,
        ),
        (
            names: [
                "--raw",
                "-r",
            ],
            description: Some("No operation, included for historical compatibility"),
        ),
        (
            names: [
                "--special-files",
                "s",
            ],
            description: Some("Normally, file only attempts to read and determine the type of\nargument files which stat(2) reports are ordinary files.  This\nprevents problems, because reading special files may have\npeculiar consequences.  Specifying the -s option causes file to\nalso read argument files which are block or character special\nfiles.  This is useful for determining the filesystem types of\nthe data in raw disk partitions, which are block special files.\nThis option also causes file to disregard the file size as\nreported by stat(2) since on some systems it reports a zero size\nfor raw disk partitions"),
        ),
        (
            names: [
                "--version",
                "-v",
            ],
            description: Some("Print the version of the program and exit"),
        ),
        (
            names: [
                "--uncompress",
                "-z",
            ],
            description: Some("Try to look inside compressed files"),
        ),
        (
            names: [
                "--uncompress-noreport",
                "-Z",
            ],
            description: Some("Try to look inside compressed files, but report information about\nthe contents only not the compression"),
        ),
        (
            names: [
                "--print0",
                "-0",
            ],
            description: Some("Output a null character '\u{0}' after the end of the filename.  Nice\nto cut(1) the output.  This does not affect the separator, which\nis still printed"),
        ),
    ],
    arguments: [
        (
            name: "file",
            description: Some("File name(s)"),
            template: [
                FilePaths,
            ],
            optional: true,
            variadic: true,
        ),
    ],
    parser_directives: Some((
        options_must_precede_arguments: true,
    )),
)
